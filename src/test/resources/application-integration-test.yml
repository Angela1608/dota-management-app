spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: http://localhost:8090
          issuer-uri: http://localhost:8090

  jpa:
    properties:
      hibernate:
        default-schema: public
